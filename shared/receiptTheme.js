/** Brand colours for receipts only (PDF + preview). */
export const RECEIPT_PALETTE = {
  navy: '#141d45',
  teal: '#00a79d',
  coral: '#e9776f',
};

/** Soft fills derived from teal for boxes on white (not additional brand colours). */
export const RECEIPT_LIGHT_FILL = '#e8f6f5';
export const RECEIPT_LIGHT_BOX = '#f0fafa';
export const RECEIPT_BORDER = '#d4eeec';
